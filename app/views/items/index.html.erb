<div class="mockup-window border bg-base-300 mx-10">
  <div class="px-4 py-16 bg-base-200">

<div class="grid">
  <% @items.each do |item| %>
    <%= render 'item', item: item, mypage_id: params[:mypage_id]%>
  <% end %>
</div>
</div>
</div>
<div class="mt-10">
<%= render "form", mypage: @mypage, item: @item %>
</div>