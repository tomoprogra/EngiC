<div class="container mx-auto flex flex-col justify-center items-center mt-10">
  <h2>アカウント名を変更できます</h2>
  <h2>現在マイページ:https://engic.dev/<%="#{@user.username}"%></h2>
  <%= form_for(@user, url: user_update_username_path, html: { class: "w-full max-w-sm" }) do |f| %>
    <div class="flex items-center border-b border-slate-400 py-2">
      <%= f.text_field :username, class:"appearance-none bg-transparent border-none w-full text-gray-700 mr-3 py-1 px-2 leading-tight focus:outline-none", placeholder:"アカウント名" %>
      <%= f.submit "変更する", class:"flex-shrink-0 bg-slate-400 hover:bg-slate-600 border-slate-400 hover:border-slate-600 text-sm border-4 text-white py-1 px-2 rounded" %>
    </div>
  <% end %>
  <%= link_to 'アカウントを削除', user_path(current_user), data: {turbo_method: :delete, turbo_confirm: '本当にアカウントを削除してもよろしいですか？' }, class: "btn btn-outline btn-error mt-16" %>
</div>
