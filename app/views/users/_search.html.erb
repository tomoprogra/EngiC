<% @users.each do |user| %>
  <li class="w-80 flex z-40 items-center gap-x-3.5 py-2 px-3 rounded-md text-sm text-gray-800 hover:bg-blue-200 focus:ring-2 focus:ring-blue-500 " role="option" data-autocomplete-value="<%= user.name %>" data-autocomplete-label="<%= user.name %>">
    <%= user.name %>
  </li>
<% end %>
